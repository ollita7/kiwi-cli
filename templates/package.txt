{
  "name": "{name}",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "compile": "kc build",
    "start": "npm run compile && node {path}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "kiwi-server-cli": "{kc}",
    "typescript": "{t}",
    "ts-node": "{tsn}",
    "@types/node": "{tnv}",
    "kiwi-server": "{kv}"
  }
}